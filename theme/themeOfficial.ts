import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#ffb74d',
    },
  },
  typography: {
    fontFamily: 'Inter',
  },
});

export default theme;
